import 'package:flutter/material.dart';

class ShieldView extends StatelessWidget {
  const ShieldView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Shield View'),
    );
  }
}
